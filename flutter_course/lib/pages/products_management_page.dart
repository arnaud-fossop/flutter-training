import 'package:flutter/material.dart';

class ProductsManagementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Manage products'),
      ),
      body: Center(
        child: Text('To manage our products'),
      ),
    );
  }
}
